import React from 'react';
import { Title, LogoWrapper } from './Logo.styled';

const Logo = () => {
    return (
        <LogoWrapper>
            <Title>Шафран</Title>
        </LogoWrapper>
    );
};

export default Logo;
