import React from 'react';
import { NavBarStyled } from './NavBar.styled';

const NavBar = () => {
    return (
        <NavBarStyled>
            <h2>NavBar</h2>
        </NavBarStyled>
    );
};

export default NavBar;
