import { FC } from 'react';
import { Main } from '../../components/Main';
import { Hero } from '../../components/Hero';

const Home: FC = () => {
    return (
        <Main>
            <Hero />
        </Main>
    );
};

export default Home;
