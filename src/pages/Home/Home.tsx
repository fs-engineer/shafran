import { FC } from 'react';
import { Main } from '../../components/Main';

const Home: FC = () => {
    return (
        <Main>
            <h2>Home</h2>
        </Main>
    );
};

export default Home;
